# ==========================================
# PIM - Configuración Docker Compose
# ==========================================
# Copia este archivo como .env y configura tus valores

# ==========================================
# Base de Datos
# ==========================================
DB_ROOT_PASSWORD=rootpassword_change_me
DB_NAME=pim_db
DB_USER=pim_user
DB_PASS=pim_secure_password_123
DB_CHARSET=utf8mb4

# ==========================================
# Aplicación
# ==========================================
APP_PORT=8080
APP_ENV=production
APP_DEBUG=false

# JWT Secret (cambiar en producción)
JWT_SECRET=change-this-to-a-random-secret-key-min-32-chars

# ==========================================
# Open WebUI (Opcional - solo si usas IA)
# ==========================================
OPENWEBUI_API_KEY=your-openwebui-api-key
OPENWEBUI_URL=http://openwebui:3000
WEBUI_SECRET_KEY=change-this-webui-secret-key
WEBUI_AUTH=true

# ==========================================
# Ollama (Opcional - solo si usas IA local)
# ==========================================
OLLAMA_URL=http://ollama:11434

# ==========================================
# NOTAS DE USO
# ==========================================
# 1. Renombrar este archivo a .env
# 2. Cambiar TODAS las contraseñas por valores seguros
# 3. Generar un JWT_SECRET aleatorio de al menos 32 caracteres
# 4. Para usar servicios de IA, ejecutar: docker-compose --profile ai up -d
# 5. Sin IA, ejecutar: docker-compose up -d
